# 🧪 캠페인 브리프 제출 및 S3 저장 테스트 가이드

## 📋 목차

1. [개요](#개요)
2. [사전 준비](#사전-준비)
3. [테스트 시나리오](#테스트-시나리오)
4. [테스트 실행](#테스트-실행)
5. [테스트 스크립트 가이드](#테스트-스크립트-가이드)
6. [결과 검증](#결과-검증)
7. [정합성 확인](#정합성-확인)
8. [문제 해결](#문제-해결)

---

## 개요

이 문서는 광고주 클라이언트(Next.js 웹앱)에서 캠페인 브리프를 제출하고, AWS S3에 저장된 결과를 확인하는 전체 테스트 프로세스를 설명합니다.

### 테스트 목표

- ✅ 캠페인 브리프 폼 제출 기능 검증
- ✅ S3에 JSON 파일이 정상적으로 저장되는지 확인
- ✅ 저장된 파일 형식이 학습노트 요구사항과 일치하는지 검증
- ✅ 17차시 Agent 1이 분석할 수 있는 구조화된 데이터인지 확인

### 테스트 환경

- **프론트엔드**: Next.js 15 (App Router)
- **백엔드**: Next.js API Routes (`/api/brief`)
- **스토리지**: AWS S3 (`campaign-briefs/new/` 폴더)
- **테스트 데이터**: `docs/campaign_briefs_15/` 폴더의 15개 샘플 브리프 (Markdown 형식, API가 JSON으로 변환하여 저장)

---

## 사전 준비

### 1. 환경 변수 설정

프로젝트 루트에 `.env.local` 파일이 있어야 하며, 다음 환경 변수가 설정되어 있어야 합니다:

```bash
AWS_ACCESS_KEY_ID="AKIA..."
AWS_SECRET_ACCESS_KEY="[YourSecretKey]"
AWS_REGION="ap-northeast-2"
AWS_S3_BUCKET_NAME="[your-bucket-name]-hackers-campaigns"
```

### 2. 의존성 설치

```bash
npm install
```

필요한 패키지:
- `@aws-sdk/client-s3`: AWS S3 클라이언트
- `dotenv`: 환경 변수 로드 (devDependencies)

### 3. 개발 서버 실행

```bash
npm run dev
```

서버가 `http://localhost:3000`에서 실행되는지 확인합니다.

---

## 테스트 시나리오

### 시나리오 1: 단일 브리프 제출 (수동 테스트)

**목적**: 브라우저를 통한 폼 제출 기능 검증

**절차**:
1. 브라우저에서 `http://localhost:3000` 접속
2. 폼에 다음 정보 입력:
   - 회사명: 기가코퍼레이션
   - 담당자명: 박민수
   - 이메일: marketing@giga.co.kr
   - 제품/서비스: AI 가속 노트북 'GigaBook X1'
   - 캠페인 총 예산 (USD): 12000
   - 목표 CPM: 20000
   - 목표 CTR: 3.0
   - 핵심 타겟 인구통계: 20-30대 남성 (MALE_20-30)
   - 상세 내용: AI 업무자동화와 개발자 워크플로우를 중심으로 성능을 보여주는 인플루언서 리뷰를 원합니다.
3. "브리프 제출하기" 버튼 클릭
4. 성공 메시지 확인 및 `/success` 페이지로 리다이렉트 확인

**예상 결과**:
- ✅ 폼 제출 성공
- ✅ S3에 `brief-form-{timestamp}-{uuid}.json` 파일 생성
- ✅ 파일이 `campaign-briefs/new/` 폴더에 저장됨

### 시나리오 2: 대량 브리프 제출 (자동화 테스트)

**목적**: `campaign_briefs_15` 폴더의 모든 샘플 브리프를 일괄 제출

**절차**:
```bash
node scripts/submit_campaigns.mjs
```

**예상 결과**:
- ✅ 15개 파일 모두 성공적으로 제출
- ✅ 각 파일에 대해 HTTP 201 응답 수신
- ✅ 생성된 파일명이 응답에 포함됨

---

## 테스트 실행

### 1. 대량 브리프 제출 스크립트 실행

```bash
cd /Users/joosung/source/hackers/final-project/match-youtubers-front

# 기본 포트 3000 사용
node scripts/submit_campaigns.mjs

# 다른 포트 사용 (예: 3001)
node scripts/submit_campaigns.mjs --port=3001
# 또는
node scripts/submit_campaigns.mjs -p 3001
# 또는 환경 변수 사용
PORT=3001 node scripts/submit_campaigns.mjs
```

**스크립트 기능**:
- `docs/campaign_briefs_15/` 폴더의 모든 `.md` 파일 자동 파싱
- Markdown 형식을 JSON으로 변환하여 API에 제출
- API가 JSON 파일로 S3에 저장 (`.json` 확장자)
- 각 요청 결과 출력 (성공/실패, 생성된 파일명)

**실행 예시 출력**:
```
🚀 Starting campaign brief submissions...
API URL: http://localhost:3000/api/brief
Briefs directory: /Users/.../docs/campaign_briefs_15

🔍 API 서버 연결 확인 중...
✅ API 서버 연결 확인 완료

✅ Success: brief_form_01_ai_laptop_review.md (HTTP 201)
   File: brief-form-1762900825655-fc32f3c6-3ad9-4af9-8840-8f9c27d7e302.json
✅ Success: brief_form_02_k_beauty_skin.md (HTTP 201)
   File: brief-form-1762900826361-1d2133f7-b8e4-4418-a5ef-b9d3ebbc499b.json
...

✨ All submissions completed!
   Success: 15
   Failed: 0
   Total: 15
```

### 2. S3 파일 목록 확인

```bash
node scripts/check_s3_briefs.mjs
```

**스크립트 기능**:
- `campaign-briefs/new/` 폴더의 파일 목록 조회
- `campaign-briefs/processed/` 폴더의 파일 목록 조회
- 각 파일의 메타데이터 표시 (파일명, 생성일, 크기)
- 처음 3개 파일의 주요 정보 파싱 및 표시 (JSON 파싱)
- 통계 요약 (파일 개수, 총 용량)

**실행 예시 출력**:
```
🔍 S3 Campaign Briefs 확인 중...
📦 Bucket: elanvital-hackers-campaigns
🌍 Region: ap-northeast-2

📁 [1] campaign-briefs/new/ 폴더 확인
────────────────────────────────────────────────────────────
   ✅ 총 27개의 파일 발견

   [1] brief-form-1762900806468-1f1ab78c-6ced-4539-8d03-ee9226a6e294.json
       📅 생성일: 2025. 11. 12. 오전 7:40:06
       📏 크기: 0.45 KB
       🔑 Key: campaign-briefs/new/brief-form-1762900806468-1f1ab78c-6ced-4539-8d03-ee9226a6e294.json
       📋 회사명: 루미에르코스메틱
       📋 제품명: 하이알루론 산뜻세럼
       💰 예산: 18000
       👥 타겟: FEMALE_20-30

...

📊 [3] 통계 요약
────────────────────────────────────────────────────────────
   📝 새 파일 (new/): 27개
   ✅ 처리 완료 (processed/): 0개
   📦 총 파일: 27개
   💾 총 용량: 17.09 KB (0.02 MB)

✨ 확인 완료!
```

---

## 결과 검증

### 1. 파일 형식 검증

학습노트에서 요구하는 JSON 형식:

```json
{
  "companyName": "기가코퍼레이션",
  "advertiserName": "박민수",
  "email": "marketing@giga.co.kr",
  "productName": "AI 가속 노트북 'GigaBook X1'",
  "budgetUSD": 12000,
  "targetDemographics": "MALE_20-30",
  "targetCPM": 20000,
  "targetCTR": 3.0,
  "details": "AI 업무자동화와 개발자 워크플로우를 중심으로 성능을 보여주는 인플루언서 리뷰를 원합니다."
}
```

### 2. 필수 필드 검증

다음 9개 필드가 모두 포함되어야 합니다:

1. ✅ 회사명
2. ✅ 담당자명
3. ✅ 이메일
4. ✅ 제품/서비스
5. ✅ 캠페인 총 예산 (USD)
6. ✅ 핵심 타겟 인구통계
7. ✅ 목표 CPM
8. ✅ 목표 CTR
9. ✅ 상세 내용

### 3. 파일명 형식 검증

**요구 형식**: `brief-form-{timestamp}-{uuid}.json`

**예시**:
- ✅ `brief-form-1762900806468-1f1ab78c-6ced-4539-8d03-ee9226a6e294.json`
- ✅ `brief-form-1762900807105-2c6e6dd9-67a5-4e6a-bb49-f66ec733703e.json`

**검증 항목**:
- `brief-form-` 접두사
- 타임스탬프 (13자리 숫자)
- 하이픈 구분자
- UUID (36자리)
- `.json` 확장자

---

## 정합성 확인

### 정합성 검증 스크립트 실행

```bash
node scripts/analyze_s3_format.mjs
```

**스크립트 기능**:
- S3에서 처음 3개 파일을 읽어서 분석
- 입력값(제출된 JSON)과 출력값(S3 저장된 JSON) 비교
- 학습노트 요구사항과 실제 파일 형식 비교
- 9개 필수 필드 검증 자동 체크

### 검증 항목 (9개 필수 필드)

1. ✅ 회사명 필드 (`companyName`): 값 존재
2. ✅ 담당자명 필드 (`advertiserName`): 값 존재
3. ✅ 이메일 필드 (`email`): 값 존재
4. ✅ 제품명 필드 (`productName`): 값 존재
5. ✅ 예산 필드 (`budgetUSD`): 숫자 타입, 값 존재
6. ✅ 인구통계 필드 (`targetDemographics`): 값 존재 (ENUM 형식)
7. ✅ CPM 필드 (`targetCPM`): 숫자 타입, 값 존재
8. ✅ CTR 필드 (`targetCTR`): 숫자 타입, 값 존재
9. ✅ 상세내용 필드 (`details`): 값 존재

### 정합성 확인 결과 예시

**Collection 1: 루미에르코스메틱**

**입력값 (제출된 JSON)**:
```json
{
  "advertiserName": "최서현",
  "companyEmail": "pr@lumiere.com",
  "companyName": "루미에르코스메틱",
  "productName": "하이알루론 산뜻세럼",
  "budgetUsd": "18000",
  "targetCpm": "25000",
  "targetCtr": "4.0",
  "targetDemographics": "FEMALE_20-30",
  "details": "수부지 피부를 위한 수분 레이어링 루틴에 자연스럽게 녹여낸 협찬 영상을 선호합니다."
}
```

**출력값 (S3 저장된 JSON)**:
```json
{
  "companyName": "루미에르코스메틱",
  "advertiserName": "최서현",
  "email": "pr@lumiere.com",
  "productName": "하이알루론 산뜻세럼",
  "budgetUSD": 18000,
  "targetDemographics": "FEMALE_20-30",
  "targetCPM": 25000,
  "targetCTR": 4.0,
  "details": "수부지 피부를 위한 수분 레이어링 루틴에 자연스럽게 녹여낸 협찬 영상을 선호합니다."
}
```

**검증 결과**: ✅ 모든 항목 통과

### 정상값 판단 근거

#### 1. 학습노트 JSON 형식 준수
- 16차시 학습노트에서 요구한 JSON 형식을 정확히 따름
- 모든 필수 필드가 포함되어 있음
- 숫자 필드(`budgetUSD`, `targetCPM`, `targetCTR`)가 숫자 타입으로 저장됨
- 문자열 필드가 올바르게 저장됨

#### 2. 17차시 Agent 1 분석 가능 형식
- Agent 1이 요구하는 9가지 필드 모두 포함
- Agent 1이 JSON을 직접 파싱하여 사용 가능 (Markdown 파싱 불필요)
- `details` 필드를 LLM으로 요약하여 `summary` 생성 가능
- 각 필드가 명확히 구분되어 파싱 가능

#### 3. 정량 목표 데이터 정확성
- **예산**: USD 달러 기준으로 명확히 표시 (`$18000`)
- **인구통계**: ENUM 형식 (`FEMALE_20-30`, `MALE_20-30` 등)
- **CPM/CTR**: 숫자 값이 명확히 표시됨 (`25000원`, `4.0%`)
- Agent 2의 SQL 필터링에 사용 가능한 형식

#### 4. 파일명 형식 준수
- 형식: `brief-form-{timestamp}-{uuid}.json`
- 고유성 보장 (timestamp + UUID)
- S3 경로: `campaign-briefs/new/` 폴더에 저장

#### 5. 데이터 무결성
- 모든 필수 필드가 채워져 있음
- 빈 값이나 null 값 없음
- 특수문자 이스케이프 처리 정상

---

## 문제 해결

### 문제 1: 환경 변수 오류

**증상**:
```
❌ Error: 다음 환경 변수가 설정되지 않았습니다:
   - AWS_ACCESS_KEY_ID
```

**해결 방법**:
1. `.env.local` 파일이 프로젝트 루트에 있는지 확인
2. 환경 변수 이름이 정확한지 확인 (대소문자 구분)
3. `.env.local` 파일에 따옴표 없이 값만 입력

### 문제 2: S3 접근 권한 오류

**증상**:
```
❌ Error: Access Denied
```

**해결 방법**:
1. AWS IAM 사용자 권한 확인 (`AmazonS3FullAccess` 권한 필요)
2. S3 버킷 정책 확인
3. `AWS_ACCESS_KEY_ID`와 `AWS_SECRET_ACCESS_KEY`가 올바른지 확인

### 문제 3: API 서버 미실행

**증상**:
```
❌ Error: connect ECONNREFUSED 127.0.0.1:3000
```

**해결 방법**:
```bash
# 개발 서버 실행 확인
npm run dev

# 포트 확인 (기본 3000)
lsof -i -P | grep LISTEN | grep 3000

# 다른 포트로 스크립트 실행
node scripts/submit_campaigns.mjs --port=<실제_포트번호>
```

### 문제 4: 파일 파싱 오류

**증상**:
```
❌ Failed to parse required fields in: ...
```

**해결 방법**:
1. Markdown 파일 형식이 학습노트 템플릿과 일치하는지 확인
2. 필수 필드가 모두 포함되어 있는지 확인
3. 특수문자(작은따옴표 등)가 올바르게 이스케이프되었는지 확인

---

## 테스트 체크리스트

### 기본 기능 테스트
- [ ] 단일 브리프 제출 성공
- [ ] 대량 브리프 제출 성공 (15개)
- [ ] S3에 파일 저장 확인
- [ ] 파일명 형식 확인 (`brief-form-{timestamp}-{uuid}.json`)

### 형식 검증 테스트
- [ ] JSON 형식 준수
- [ ] 필수 필드 9개 모두 포함
- [ ] 숫자 필드가 숫자 타입으로 저장됨
- [ ] ENUM 형식 인구통계 값 확인

### 정합성 테스트
- [ ] 입력값(JSON)과 출력값(JSON) 일치
- [ ] 데이터 무결성 확인 (빈 값 없음)
- [ ] Agent 1 JSON 파싱 가능 형식 확인
- [ ] Agent 2 SQL 필터링 호환 형식 확인

### 통합 테스트
- [ ] 17차시 Agent 1 폴링 준비 완료
- [ ] `campaign-briefs/new/` 폴더에 파일 저장 확인
- [ ] 파일 내용 파싱 가능 확인

---

## 테스트 스크립트 가이드

프로젝트에는 다양한 테스트 및 검증을 위한 스크립트들이 포함되어 있습니다. 각 스크립트의 사용법과 기능을 설명합니다.

### 1. `submit_campaigns.mjs` - 대량 브리프 제출

**목적**: `docs/campaign_briefs_15/` 폴더의 모든 샘플 브리프를 일괄 제출 (또는 1개씩 제출)

**사용법**:
```bash
# 모든 미제출 파일 제출 (기본 포트 3000)
node scripts/submit_campaigns.mjs

# 포트 번호 지정 (--port=3001 또는 -p 3001)
node scripts/submit_campaigns.mjs --port=3001
node scripts/submit_campaigns.mjs -p 3001

# 1개만 제출 (pop 방식)
node scripts/submit_campaigns.mjs --one
# 또는
node scripts/submit_campaigns.mjs -1

# 포트 지정과 함께 1개만 제출
node scripts/submit_campaigns.mjs --one --port=3001

# 제출 이력 초기화
node scripts/submit_campaigns.mjs --reset
# 또는
node scripts/submit_campaigns.mjs -r

# 환경 변수로 포트 지정
PORT=3001 node scripts/submit_campaigns.mjs

# 환경 변수로 전체 API URL 지정
API_URL=http://localhost:3001/api/brief node scripts/submit_campaigns.mjs
```

**기능**:
- `campaign_briefs_15/` 폴더의 모든 `.md` 파일 자동 파싱
- Markdown 형식을 JSON으로 변환하여 API에 제출
- API가 JSON 파일로 S3에 저장 (`.json` 확장자)
- `POST /api/brief` API에 curl로 제출
- 각 요청 결과 출력 (성공/실패, 생성된 파일명)
- **제출 이력 추적**: `.submitted_briefs.json` 파일에 제출 완료된 파일 저장
- **중복 방지**: 이미 제출한 파일은 자동으로 제외
- **1개씩 제출 모드**: `--one` 옵션으로 한 번에 1개만 제출 (pop 방식)
- API 부하 방지를 위한 0.5초 딜레이

**환경 변수**:
- `API_URL`: API 엔드포인트 URL (기본값: `http://localhost:3000/api/brief`)
- `PORT`: 포트 번호 지정 (기본값: `3000`, `API_URL`이 설정되면 무시됨)

**상태 파일**:
- `.submitted_briefs.json`: 제출 완료된 파일 목록 저장 (프로젝트 루트)

**예시 출력 (일반 모드)**:
```
🚀 Starting campaign brief submissions...
API URL: http://localhost:3000/api/brief
Briefs directory: /Users/.../docs/campaign_briefs_15

📋 이미 제출된 파일: 5개
   brief_form_01_ai_laptop_review.md, brief_form_02_k_beauty_skin.md, ...

📝 제출 대기 중인 파일: 10개

✅ Success: brief_form_03_mobile_game_launch.md (HTTP 201)
   File: brief-form-1762900825655-fc32f3c6-3ad9-4af9-8840-8f9c27d7e302.json
...

✨ All submissions completed!
   Success: 10
   Failed: 0
   Processed: 10

📊 제출 상태 요약:
   총 파일: 15개
   제출 완료: 15개
   남은 파일: 0개
```

**예시 출력 (1개씩 제출 모드)**:
```
🚀 Starting campaign brief submissions...
API URL: http://localhost:3000/api/brief
Briefs directory: /Users/.../docs/campaign_briefs_15
📌 Mode: ONE (1개만 제출)

📋 이미 제출된 파일: 3개
   brief_form_01_ai_laptop_review.md, brief_form_02_k_beauty_skin.md, ...

📝 제출 대기 중인 파일: 12개

🎯 처리할 파일: brief_form_04_fintech_budget_app.md

✅ Success: brief_form_04_fintech_budget_app.md (HTTP 201)
   File: brief-form-1762900825655-fc32f3c6-3ad9-4af9-8840-8f9c27d7e302.json

✨ 1개 제출 완료!
   남은 파일: 11개

📊 제출 상태 요약:
   총 파일: 15개
   제출 완료: 4개
   남은 파일: 11개

💡 다음 파일을 제출하려면 다시 실행하세요:
   node scripts/submit_campaigns.mjs --one
```

---

### 2. `check_s3_briefs.mjs` - S3 파일 목록 확인

**목적**: S3의 `campaign-briefs/new/` 및 `campaign-briefs/processed/` 폴더의 파일 목록 확인

**사용법**:
```bash
node scripts/check_s3_briefs.mjs
```

**기능**:
- `campaign-briefs/new/` 폴더의 파일 목록 조회
- `campaign-briefs/processed/` 폴더의 파일 목록 조회
- 각 파일의 메타데이터 표시 (파일명, 생성일, 크기, S3 Key)
- 처음 3개 파일의 주요 정보 파싱 및 표시 (JSON 파싱하여 회사명, 제품명, 예산, 타겟 추출)
- 통계 요약 (파일 개수, 총 용량)

**필수 환경 변수**:
- `AWS_ACCESS_KEY_ID`
- `AWS_SECRET_ACCESS_KEY`
- `AWS_REGION`
- `AWS_S3_BUCKET_NAME`

**예시 출력**:
```
🔍 S3 Campaign Briefs 확인 중...
📦 Bucket: elanvital-hackers-campaigns
🌍 Region: ap-northeast-2

📁 [1] campaign-briefs/new/ 폴더 확인
────────────────────────────────────────────────────────────
   ✅ 총 27개의 파일 발견

   [1] brief-form-1762900806468-1f1ab78c-6ced-4539-8d03-ee9226a6e294.json
       📅 생성일: 2025. 11. 12. 오전 7:40:06
       📏 크기: 0.45 KB
       🔑 Key: campaign-briefs/new/brief-form-...
       📋 회사명: 루미에르코스메틱
       📋 제품명: 하이알루론 산뜻세럼
       💰 예산: 18000
       👥 타겟: FEMALE_20-30

📊 [3] 통계 요약
────────────────────────────────────────────────────────────
   📝 새 파일 (new/): 27개
   ✅ 처리 완료 (processed/): 0개
   📦 총 파일: 27개
   💾 총 용량: 17.09 KB (0.02 MB)
```

---

### 3. `analyze_s3_format.mjs` - 형식 및 정합성 검증

**목적**: S3에 저장된 파일의 입력값과 출력값을 비교하여 정합성 검증

**사용법**:
```bash
node scripts/analyze_s3_format.mjs
```

**기능**:
- S3에서 처음 3개 파일을 읽어서 분석
- 입력값(제출된 JSON)과 출력값(S3 저장된 JSON) 비교
- 학습노트 요구사항과 실제 파일 형식 비교
- 9개 필수 필드 검증 자동 체크:
  - 필수 필드 9개 (회사명, 담당자명, 이메일, 제품명, 예산, 인구통계, CPM, CTR, 상세내용)
  - 숫자 필드 타입 검증 (`budgetUSD`, `targetCPM`, `targetCTR`)
- 정상값 판단 근거 설명

**예시 출력**:
```
📋 S3 저장 파일 형식 분석 및 학습노트 비교
================================================================================

📄 Collection 1: brief-form-1762900806468-...
────────────────────────────────────────────────────────────────────────────────

**1. 입력값 (제출된 JSON 형식):**
{
  "advertiserName": "최서현",
  "companyEmail": "pr@lumiere.com",
  ...
}

**2. 출력값 (S3에 저장된 JSON 파일):**
{
  "companyName": "루미에르코스메틱",
  ...
}

**4. 형식 검증 결과:**
   ✅ 제목 형식: 통과
   ✅ 섹션 1 존재: 통과
   ...
   **전체 검증 결과: ✅ 정상**
```

---

### 4. `compare_format.mjs` - 학습노트 JSON 형식 비교

**목적**: 학습노트에서 요구하는 JSON 형식과 실제 S3 저장 파일의 형식을 비교

**사용법**:
```bash
node scripts/compare_format.mjs
```

**기능**:
- 학습노트 요구 JSON 형식 표시
- 실제 S3 저장 JSON 파일 표시
- 형식 비교 분석 (필드명, 타입, 구조 등)
- **중요**: JSON 형식의 정확성과 필수 필드 포함 여부를 검증

**예시 출력**:
```
📋 학습노트 템플릿 vs 실제 S3 저장 파일 형식 비교
================================================================================

📚 [학습노트 요구 JSON 형식]
{
  "companyName": "...",
  "budgetUSD": 12000,
  ...
}

📄 [실제 S3 저장 JSON 파일]
{
  "companyName": "...",
  "budgetUSD": 18000,
  ...
}

🔍 [형식 비교 분석]
────────────────────────────────────────────────────────────
✅ 필수 필드 9개 모두 포함
✅ 숫자 필드 타입 정확 (budgetUSD, targetCPM, targetCTR)
✅ ENUM 형식 인구통계 값 확인
✅ JSON 형식 유효성 검증

✅ **결론: 학습노트 요구사항을 정확히 준수합니다!**
**중요**: JSON 형식이 정확하고 필수 필드가 모두 포함되어 있으면 Agent 1이 정상적으로 분석할 수 있습니다.
```

---

### 5. `get_raw_brief.mjs` - RAW JSON 파일 내용 출력

**목적**: S3에 저장된 JSON 파일 하나를 RAW 형태로 정확하게 출력

**사용법**:
```bash
node scripts/get_raw_brief.mjs
```

**기능**:
- S3에서 첫 번째 파일을 읽어서 RAW 형태로 출력
- 파일명 및 S3 Key 정보 표시
- JSON 파일 내용을 그대로 출력 (가공 없음, 포맷팅 포함)

**예시 출력**:
```
파일명: brief-form-1762900806468-1f1ab78c-6ced-4539-8d03-ee9226a6e294.json
S3 Key: campaign-briefs/new/brief-form-1762900806468-...
────────────────────────────────────────────────────────────

RAW 내용:
────────────────────────────────────────────────────────────

{
  "companyName": "루미에르코스메틱",
  "advertiserName": "최서현",
  "email": "pr@lumiere.com",
  ...
}
```

---

### 6. `generate_campaigns.mjs` - 캠페인 테이블 합성 데이터 생성

**목적**: Supabase `campaigns` 테이블에 Flutter 앱 테스트용 합성 데이터 생성

**사용법**:
```bash
# 기본 5개 생성
node scripts/generate_campaigns.mjs

# 10개 생성
node scripts/generate_campaigns.mjs --count=10

# 기존 데이터 삭제 후 생성
node scripts/generate_campaigns.mjs --wipe

# 조합 사용
node scripts/generate_campaigns.mjs --count=10 --wipe
```

**기능**:
- Supabase `campaigns` 테이블에 다양한 상태의 합성 캠페인 데이터 생성
- **다양한 상태 지원**:
  - `pending`: AI 분석 전 상태
  - `pending_approval`: AI 분석 완료, 승인 대기
  - `pending_proposal_approval`: 제안서 생성 완료, 승인 대기
  - `completed`: 완료된 캠페인
- **합성 데이터 포함**:
  - `ai_analysis`: 회사명, 제품명, 예산, 타겟 인구통계 등
  - `matched_youtubers`: 매칭된 유튜버 정보 (제안서 단계) - **단일 JSON 객체** (Agent 2가 여러 후보를 분석한 후 최적 1명 선택)
  - `generated_proposal`: 제안서 마크다운 텍스트 (복수 후보인 경우 "후보 비교 분석" 섹션 포함)
  - `generated_contract`: 계약서 마크다운 텍스트 (완료 단계)
- Flutter 앱의 실시간 업데이트 및 다양한 상태 UI 테스트에 활용

**환경 변수** (`.env.local` 파일):
```bash
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_KEY="your_service_role_key"
```

**필수 의존성**:
- `@supabase/supabase-js`: Supabase 클라이언트
- `uuid`: UUID 생성
- `dotenv`: 환경 변수 로드

**예시 출력**:
```
🔄 Generating 5 campaigns...

✅ Successfully inserted 5 campaigns:

1. pending                    | campaign-briefs/new/brief-form-...
2. pending_approval            | campaign-briefs/new/brief-form-...
3. pending_proposal_approval   | campaign-briefs/new/brief-form-...
4. completed                   | campaign-briefs/new/brief-form-...
5. pending_approval            | campaign-briefs/new/brief-form-...

🎉 Done! Generated 5 campaigns.

💡 Tips:
   - Flutter 앱에서 실시간 업데이트를 확인하세요
   - 다양한 status 값을 테스트할 수 있습니다
   - --wipe 옵션으로 기존 데이터를 삭제할 수 있습니다
```

**사용 시나리오**:
- Flutter 앱 개발 시 다양한 상태의 캠페인 데이터가 필요할 때
- 실시간 업데이트 기능 테스트
- 상태별 UI 렌더링 테스트
- 워크플로우 테스트 (승인 프로세스 등)

---

### 스크립트 사용 시나리오

#### 시나리오 1: 전체 테스트 플로우
```bash
# 1. 대량 브리프 제출 (기본 포트 3000)
node scripts/submit_campaigns.mjs

# 다른 포트 사용 시
node scripts/submit_campaigns.mjs --port=3001

# 2. S3 파일 목록 확인
node scripts/check_s3_briefs.mjs

# 3. 형식 및 정합성 검증
node scripts/analyze_s3_format.mjs
```

#### 시나리오 2: 빠른 확인
```bash
# S3 파일 목록만 확인
node scripts/check_s3_briefs.mjs

# 특정 파일의 RAW 내용 확인
node scripts/get_raw_brief.mjs
```

#### 시나리오 3: 형식 검증
```bash
# 학습노트 템플릿과 비교
node scripts/compare_format.mjs

# 상세 정합성 분석
node scripts/analyze_s3_format.mjs
```

#### 시나리오 4: Flutter 앱 테스트용 데이터 생성
```bash
# 캠페인 테이블에 합성 데이터 생성
node scripts/generate_campaigns.mjs

# 기존 데이터 삭제 후 재생성
node scripts/generate_campaigns.mjs --wipe

# 10개 생성
node scripts/generate_campaigns.mjs --count=10
```

---

### 스크립트 공통 요구사항

**S3 관련 스크립트 필수 환경 변수** (`.env.local` 파일):
```bash
AWS_ACCESS_KEY_ID="AKIA..."
AWS_SECRET_ACCESS_KEY="[YourSecretKey]"
AWS_REGION="ap-northeast-2"
AWS_S3_BUCKET_NAME="[your-bucket-name]-hackers-campaigns"
```

**Supabase 관련 스크립트 필수 환경 변수** (`.env.local` 파일):
```bash
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_KEY="your_service_role_key"
```

**필수 의존성**:
- `@aws-sdk/client-s3`: AWS S3 클라이언트 (S3 관련 스크립트)
- `@supabase/supabase-js`: Supabase 클라이언트 (Supabase 관련 스크립트)
- `uuid`: UUID 생성 (`generate_campaigns.mjs`)
- `dotenv`: 환경 변수 로드 (devDependencies)

**실행 권한**:
모든 스크립트는 실행 가능하도록 설정되어 있습니다:
```bash
chmod +x scripts/*.mjs
```

---

## 참고 자료

### 관련 문서
- [16차시 학습노트](./dbsetup/16차시%20학습노트.md): 인프라 및 클라이언트 구축
- [17차시 학습노트](./dbsetup/17차시%20학습노트.md): AI Agent 구현
- [PRD.md](./PRD.md): 프로젝트 요구사항 문서

### 샘플 데이터
- `docs/campaign_briefs_15/`: 15개 샘플 캠페인 브리프

---

## 결론

이 테스트 가이드를 통해 다음을 확인할 수 있습니다:

1. ✅ **기능 검증**: 캠페인 브리프 제출 기능이 정상 작동함
2. ✅ **형식 검증**: 저장된 JSON 파일이 학습노트 요구사항을 준수함
3. ✅ **정합성 검증**: 입력값과 출력값이 일치하며 데이터 무결성이 보장됨
4. ✅ **Agent 호환성**: 17차시 Agent 1이 JSON을 직접 파싱하여 분석할 수 있는 구조화된 형식임

모든 테스트를 통과한 JSON 파일들은 17차시 Agent 1의 폴링 대상이 되어 자동으로 분석됩니다.

