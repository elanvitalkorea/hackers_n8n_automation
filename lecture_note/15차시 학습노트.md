
## 🎯 이번 차시의 목표

n8n을 통해 웹과 백엔드 자동화에 익숙해진 우리가, 이제 모바일 앱 개발의 세계로 마지막 단계를 내딛습니다. 15차시는 n8n 자동화의 클라이언트(Client)를 모바일로 확장하는 경험을 제공하기 위해 설계되었습니다.

- **학습 목표:**
    1. Flutter와 Dart의 핵심 개념을 JavaScript와 비교하여 빠르게 이해합니다.
    2. Flutter 앱에서 외부 API(Upstage OCR)를 연동하는 방법을 배웁니다.
    3. 모바일 앱을 n8n 백엔드 워크플로우(Webhook)와 연결하여 E2E(End-to-End) 자동화 파이프라인을 완성합니다.
    4. 웹, 백엔드(n8n), 모바일(Flutter)까지 섭렵하여 어떤 개발이든 가능한 "Full-Stack 자동화 개발자"로 성장합니다.
        

## 15-1. Flutter 소개 및 셋업 (25분)

### 1. Flutter란 무엇인가? (2분)

Flutter는 Google이 2017년에 출시한 UI 툴킷입니다. n8n이 Node.js 기반이듯, Flutter는 **Dart**라는 언어를 기반으로 합니다.

- **Flutter의 역사:** Google에서 개발하였으며, 아름다운 UI와 네이티브 성능을 목표로 탄생했습니다.
    
- **Native 앱 대비 장점:**
    
    - **단일 코드베이스(Single Codebase):** Dart 언어 하나로 iOS, Android, Web, Desktop 앱을 모두 만들 수 있습니다. (엄청난 생산성!)
    - **빠른 개발 속도 (Hot Reload):** 코드를 수정하면 몇 초 안에 앱 화면에 즉시 반영됩니다.
    - 스타트업 대다수가 Flutter로 빠른 MVP 진행 
        
    - **아름다운 UI:** 모든 것을 '위젯(Widget)'으로 그려내어, 어떤 플랫폼에서든 일관되고 아름다운 디자인을 보장합니다.
    - 
- **Anatomy (구조):**
    ![hackers_15th_01](https://hackers-n8n-lecture.s3.ap-northeast-2.amazonaws.com/images/hackers_15th_01.png)
    

### 2. Flutter 설치 (3분)

Flutter 개발 환경을 설정하는 것은 Flutter SDK와 각 플랫폼(iOS/Android)별 개발 도구를 설치하는 것을 의미합니다.

- **필수 설치 요소:**
    
    1. **Flutter SDK:** [공식 홈페이지](https://flutter.dev/docs/get-started/install "null")에서 다운로드합니다.
        
    2. **Android Studio:** Android 앱을 빌드하고 'Android Emulator'(가상 폰)를 실행하기 위해 필요합니다.
        
    3. **Xcode (Mac 사용자):** iOS 앱을 빌드하고 'iOS Simulator'를 실행하기 위해 필요합니다.
        
- **설치 확인:** 터미널에서 `flutter doctor` 명령어를 실행하면, 설치가 필요한 부분이나 설정 오류를 알려줍니다.
    
- **(참고) 자동 설치:** Cursor나 Claude CLI 같은 AI 도구를 사용하면 이 과정을 자동화할 수도 있습니다. `prompt: "set up flutter development environment for me"`
    

### 3. Dart 핵심 문법 

#### 1\. JavaScript 개발자를 위한 Dart 입문

Dart는 Google이 개발한 언어로, Flutter 앱을 만드는 데 사용됩니다. 4차시에서 배운 JavaScript와 매우 유사하여 쉽게 배울 수 있습니다.

| 특징 | JavaScript (4차시 복습) | **Dart (오늘의 핵심)** |
| :--- | :--- | :--- |
| **타입 시스템** | **Dynamic Typing** (동적 타입) | **Static Typing** (정적 타입) |
| | `let name = '김철수';` | `String name = '김철수';` |
| **널(Null) 처리** | `undefined`와 `null` | **Null Safety** (널 안정성) / `?` 기호 사용 |
| **객체 지향** | 프로토타입 기반 | **클래스 기반** (Java, C\#과 유사) |
| **비동기** | `Promise` (async/await) | `Future` (async/await) |

-----

#### 2\. 변수와 타입 (Type System)

가장 큰 차이점입니다. JavaScript는 타입이 유동적이지만, Dart는 **타입을 명시적으로 선언**하여 코드 실행 전(컴파일 시)에 오류를 잡을 수 있습니다.

```dart
// 1. 타입 추론 (JavaScript의 let과 유사)
// var는 타입을 자동으로 추론하지만, 한번 정해지면 바뀌지 않습니다.
var name = '김해커'; // 'String' 타입으로 추론됨
// name = 123; // ❌ 에러!

// 2. 명시적 타입 선언 (권장!)
String company = 'Elanvital AI';
int years = 20;
double rating = 4.5;
bool isFounder = true;
```

#### 3\. 불변성: final vs const (JS의 const와 비교)

JavaScript의 `const`는 '재할당 불가'를 의미합니다. Dart는 이를 `final`과 `const`로 더 세분화합니다.

```dart
// 1. final (Runtime Constant)
// JavaScript의 const와 가장 유사합니다.
// 앱이 실행되는 동안 값이 정해지고, 그 후에는 변경할 수 없습니다.
final String nickname = '개발자';
// nickname = '기획자'; // ❌ 에러!

// 2. const (Compile-time Constant)
// 코드를 작성하는 시점(컴파일 시)에 값이 완벽하게 정해져야 합니다.
// 더 엄격하며, 성능에 이점이 있습니다.
const int MAX_USERS = 100;
const String API_KEY = 'YOUR_API_KEY'; // 하드코딩된 값
```

-----

#### 4\. 꼭 알아야 할 특별한 키워드: late, dynamic

`String`, `int` 외에 Dart의 타입 시스템을 유연하게 만드는 중요 키워드입니다.

  * **`dynamic` (동적 타입)**
      * `dynamic`은 JavaScript의 변수처럼 **모든 타입을 허용**하며, 정적 타입 검사를 비활성화합니다.
      * `var`는 타입이 추론되면 고정되지만, `dynamic`은 계속 바뀔 수 있습니다.
      * ```dart
          dynamic anything = '김해커';
          anything = 123; // ✅ 가능
          anything = true; // ✅ 가능
        ```
      * **주의**: 꼭 필요할 때(예: JSON 파싱 초기 단계) 외에는 사용을 피하는 것이 좋습니다. Dart의 강점인 타입 안정성을 포기하는 것입니다.
  * **`late` (지연 초기화)**
      * `late` 키워드는 "이 변수는 지금 당장 값은 없지만, **사용하기 직전에 반드시 초기화될 것을 약속할게\!**"라는 의미입니다.
      * **`late`가 필요한 이유**:
        1.  **Non-nullable(`?`가 없는) 변수**는 선언과 동시에 초기화해야 합니다.
        2.  하지만, `initState` (Flutter)나 클래스 생성자에서 나중에 초기화해야 하는 경우가 많습니다.
        3.  이때 `late`를 쓰면 컴파일러에게 "널(null)이 아니니 일단 에러 내지 말고 기다려 줘"라고 말할 수 있습니다.
      * ```dart
          class MyWidgetState extends State<MyWidget> {
            // '?'를 붙이면 (String? _name) 계속 null 체크를 해야 해서 번거로움
            // late를 쓰면 '?' 없이 선언 가능
            late String _userName; 
          
            @override
            void initState() {
              super.initState();
              _userName = '김해커'; // 여기서 '약속대로' 초기화
            }
          
            void printName() {
              print(_userName); // 'late'가 없었다면 "초기화되지 않았음" 에러
            }
          }
        ```

-----

#### 5\. 널 안정성 (Null Safety)

Dart의 가장 강력한 기능 중 하나입니다. "널(null) 값 때문에 앱이 죽는" 오류(Null Pointer Exception)를 원천 차단합니다.

```dart
// 1. Non-nullable (널 불가능 - 기본값)
// 변수는 기본적으로 null 값을 가질 수 없습니다.
String name = '김해커';
// name = null; // ❌ 에러!

// 2. Nullable (널 가능 - '?')
// 타입 뒤에 '?'를 붙이면 null 값을 가질 수 있습니다.
String? middleName = null; // ✅ 가능
middleName = '철수'; // ✅ 가능

// 3. 널 체크
// '?'를 붙인 변수를 사용할 때는 반드시 널 체크를 해야 합니다.
if (middleName != null) {
  print(middleName.length);
}

// 4. 안전 연산자 (?.) - 4차시 JS와 동일
// middleName이 null이면 null을 반환하고, 아니면 길이를 반환합니다.
print(middleName?.length);
```

-----

#### 6\. 함수 (Functions)

JavaScript와 매우 유사합니다. 특히 **화살표 함수(`=>`)** 문법이 동일합니다.

```dart
// 1. 기본 함수
String greet(String name) {
  return '안녕하세요, $name님!'; // JS의 Template Literal과 유사
}

// 2. 화살표 함수 (=>)
// 함수 본체가 한 줄일 때 사용합니다.
String greetArrow(String name) => '안녕하세요, $name님!';

// 3. Named Parameters (Flutter에서 가장 많이 사용!)
// 매개변수를 이름으로 전달하여 순서와 상관없이 명확하게 호출합니다.
void setupUser({required String name, int age = 30}) {
  print('$name ($age세) 님 설정 완료');
}

// 호출
setupUser(name: '김해커'); // age는 기본값 30 사용
setupUser(name: '박AI', age: 5);
// setupUser(age: 40); // ❌ 에러! 'name'은 required
```

-----

#### 7\. 클래스와 객체 (Classes & OOP)

Flutter의 모든 위젯은 **클래스**입니다. JavaScript의 `JSON` 객체를 Dart의 `Class` 객체로 변환하는 것이 핵심입니다.

```dart
// 1. 클래스 선언 (붕어빵 틀)
class User {
  // 2. 필드 (속성)
  final String name;
  final int age;

  // 3. 생성자 (Constructor) - 붕어빵 굽기
  // this.name은 'name' 매개변수를 받아서 'User'의 'name' 필드에 할당하라는 의미
  User(this.name, this.age);

  // 4. 메서드 (기능)
  void sayHello() {
    print('안녕하세요, $name입니다. $age살입니다.');
  }
}

// 5. 객체 생성 (인스턴스)
var user1 = User('김해커', 20);
user1.sayHello(); // 출력: 안녕하세요, 김해커입니다. 20살입니다.
```

-----

#### 8\. 비동기 처리: Future (async / await)

  * **`Future`란?**: JavaScript의 `Promise`와 100% 동일한 개념입니다.
  * "지금 당장은 아니지만, **미래에** 데이터(성공) 또는 오류(실패)를 돌려주겠다"는 '약속'입니다.
  * API 호출, 파일 읽기 등 시간이 걸리는 모든 작업은 `Future`를 반환합니다.
  * `async / await`는 이 '약속'이 이행될 때까지(데이터가 오거나 에러가 날 때까지) 코드를 깔끔하게 기다리게 해주는 문법입니다.


```dart
// JavaScript의 Promise == Dart의 Future
Future<String> fetchData() async {
  try {
    // await: Future가 완료될 때까지 기다림
    // 3초가 걸리는 가상의 네트워크 작업을 시뮬레이션
    var result = await Future.delayed(Duration(seconds: 3), () {
      return '데이터 로딩 성공!';
    });
    return result;
  } catch (e) {
    return '오류 발생: $e';
  }
}

// 4차시에서 배운 main 함수와 동일
void main() async {
  print('데이터 요청 시작...');
  String data = await fetchData();
  print(data); // 3초 후에 '데이터 로딩 성공!' 출력
}
```

-----

#### 9\. 컬렉션(Collection)을 위한 강력한 Syntax Sugar

Flutter는 UI를 코드로 작성(UI-as-Code)합니다. 따라서 코드 내에서 List나 Map을 선언적으로, 그리고 동적으로 생성하는 것이 매우 중요합니다. Dart는 이를 위해 JavaScript보다 훨씬 강력한 문법을 제공합니다.

##### A. Collection `if` (조건부 요소 추가)

`List`나 `Map`을 생성하는 리터럴(`[]` 또는 `{}`) 안에서 `if` 문을 사용하여 조건에 따라 요소를 포함하거나 제외할 수 있습니다.

**[ Dart 방식 ]**

```dart
bool includeAdmin = true;
var userList = [
  '김해커',
  '박AI',
  if (includeAdmin) '관리자', // if 문을 리스트 안에 직접 사용
];
// 결과: ['김해커', '박AI', '관리자']
```

**[ 4차시 JavaScript 방식 (비교) ]**

```javascript
const includeAdmin = true;
const userList = ['김해커', '박AI'];

if (includeAdmin) {
  userList.push('관리자'); // 리스트를 선언한 후에 별도로 추가해야 함
}
// 결과: ['김해커', '박AI', '관리자']
```

-----

##### B. Collection `for` (반복 요소 추가)

`List`나 `Map` 리터럴 안에서 `for` 루프를 사용하여 다른 컬렉션의 요소를 기반으로 새로운 컬렉션을 생성할 수 있습니다.

**[ Dart 방식 ]**

```dart
var numbers = [1, 2, 3];

// for...in 루프를 리스트 안에 직접 사용
var doubledList = [
  0,
  for (var n in numbers) n * 2, // n*2의 결과를 리스트 요소로 추가
];
// 결과: [0, 2, 4, 6]
```

**[ 4차시 JavaScript 방식 (비교) ]**

```javascript
const numbers = [1, 2, 3];

// map을 사용하거나 (권장)
const doubledListMap = [0, ...numbers.map(n => n * 2)];

// for...of를 사용하려면
const doubledListFor = [0];
for (const n of numbers) {
  doubledListFor.push(n * 2);
}
// 결과: [0, 2, 4, 6]
```

-----

##### C. Null-aware Spread Operator (`...?`)

4차시에서 배운 스프레드 연산자(`...`)는 Dart에도 동일하게 존재합니다.
하지만 Dart는 \*\*널 안정성(Null Safety)\*\*과 결합된 `...?` (널-인식 스프레드) 연산자를 추가로 제공합니다.

**[ Dart 방식 ]**

```dart
List<int> list1 = [1, 2, 3];
List<int>? list2 = null; // 널(null)일 수 있는 리스트
List<int> list3 = [7, 8, 9];

var combinedList = [
  ...list1,      // [1, 2, 3]을 펼침
  ...?list2,     // ⭐️ list2가 null이면 아무것도 추가하지 않음 (에러 없음!)
  ...list3,      // [7, 8, 9]을 펼침
];
// 결과: [1, 2, 3, 7, 8, 9]

// 만약 list2가 null이 아니었다면?
// list2 = [4, 5, 6];
// 결과: [1, 2, 3, 4, 5, 6, 7, 8, 9]
```

**[ 4차시 JavaScript 방식 (비교) ]**

```javascript
const list1 = [1, 2, 3];
const list2 = null;
const list3 = [7, 8, 9];

// list2가 null일 때 ... 연산자를 쓰면 에러가 발생!
// const combinedList = [...list1, ...list2, ...list3]; // ❌ TypeError!

// 에러를 피하려면 이렇게 해야 함
const combinedList = [...list1, ...(list2 || []), ...list3];
// 결과: [1, 2, 3, 7, 8, 9]
```

-----

##### D. Cascade Notation (`..`) (연속 호출)

컬렉션 자체의 문법은 아니지만, 객체나 컬렉션을 **생성**하고 **즉시** 여러 메서드를 호출(설정)할 때 압도적으로 편리한 Dart만의 문법입니다.

`..` 연산자는 메서드를 호출하되, 메서드의 반환값 대신 \*\*객체 자신(`this`)\*\*을 반환합니다.

**[ Dart 방식 (Cascade) ]**

```dart
// 리스트를 생성하고, 바로 이어서 add, sort, clear 등을 호출
var myList = <String>[]
  ..add('Flutter')
  ..add('Dart')
  ..add('n8n')
  ..sort() // 정렬
  ..remove('n8n'); // 제거

// 위 코드는 아래와 100% 동일합니다.
// var myList = <String>[];
// myList.add('Flutter');
// myList.add('Dart');
// myList.add('n8n');
// myList.sort();
// myList.remove('n8n');

print(myList); // 결과: [Dart, Flutter]
```

**[ 4차시 JavaScript 방식 (비교) ]**

```javascript
// JavaScript의 .push()는 길이를 반환하고, .sort()는 배열 자신을 반환하는 등
// 반환값이 일관되지 않아 연속 호출(Method Chaining)이 제한됩니다.
const myList = [];
myList.push('Flutter');
myList.push('Dart');
myList.push('n8n');
myList.sort();
myList.splice(myList.indexOf('n8n'), 1); // 'n8n' 제거

console.log(myList); // 결과: [Dart, Flutter]
```

-----

##### 🎯 컬렉션 문법 핵심 정리

1.  **Collection `if`**: `[ if (조건) 값 ]` → Flutter UI 코드에서 위젯을 조건부로 추가할 때 매우 유용합니다.
2.  **Collection `for`**: `[ for (var i in items) 값 ]` → 리스트를 기반으로 동적인 위젯 리스트를 만들 때 사용합니다.
3.  **`...?` (Null-aware Spread)**: `[ ...?nullableList ]` → API 응답처럼 `null`일 수 있는 리스트를 안전하게 합칠 때 사용합니다.
4.  **`..` (Cascade)**: `myList..add(1)..add(2);` → 객체/리스트 생성과 동시에 여러 설정을 연속적으로 처리할 때 코드가 깔끔해집니다.

-----

### 4. Flutter 문법 소개 

#### 1\. Flutter의 심장: 위젯 (Widget) (5분)

4차시에서 Node.js가 JavaScript 런타임 환경이었듯, Flutter는 UI를 그리기 위한 **툴킷**입니다. Flutter의 핵심 철학은 \*\*"모든 것은 위젯이다"\*\*입니다.

  * **UI 요소**: `Text`, `ElevatedButton`, `Icon`
  * **레이아웃**: `Column`(세로), `Row`(가로), `Center`(중앙)
  * **스타일**: `Padding`(여백), `Container`(배경/크기), `TextStyle`(글꼴)
  * **앱 전체**: `MaterialApp` (앱의 근간)

이 모든 것이 **위젯**입니다. Flutter 개발은 이 레고 블록 같은 위젯들을 '중첩(nesting)'해서 조립하는 과정입니다.

##### 🎨 Material vs. Cupertino: 앱의 디자인 시스템

위젯은 '기능'만 제공할 뿐 '디자인'은 제공하지 않는 경우도 있습니다. Flutter는 두 가지 완성형 디자인 시스템 위젯 세트를 기본 제공합니다.

1.  **Material (머티리얼)**
      * Google이 만든 디자인 시스템 (안드로이드 스타일).
      * `MaterialApp`, `Scaffold`(화면 뼈대), `AppBar`(상단 바), `FloatingActionButton`(하단 원형 버튼) 등이 포함됩니다.
      * 범용성이 높아 iOS 앱을 만들 때도 대부분 Material을 사용합니다.
2.  **Cupertino (쿠퍼티노)**
      * Apple이 만든 디자인 시스템 (iOS 스타일).
      * `CupertinoApp`, `CupertinoPageScaffold`, `CupertinoNavigationBar` 등이 포함됩니다.
      * iOS 순정 느낌을 강조하고 싶을 때 사용합니다.

**핵심**: 앱 개발 시, `MaterialApp`이나 `CupertinoApp` 중 하나를 앱의 최상단에 배치하여 앱 전체의 디자인 테마와 네비게이션을 결정합니다.

-----

#### 2\. StatelessWidget vs. StatefulWidget (7분)

Flutter에는 두 가지 핵심 위젯 유형이 있습니다.

##### 🖼️ StatelessWidget (상태 없는 위젯)

> "한번 그려지면 스스로 바뀌지 않는 정적인 위젯" (예: 간판, 그림)

  * **특징**: 자체적으로 변경 가능한 **'상태(State)'를 가지지 않습니다.**
  * **데이터**: 부모 위젯으로부터 데이터를 전달받아(`final String name;`) 화면을 그리기만 합니다.
  * **생명주기**: `build()` 메서드만 가집니다.
  * **예시**: `Text('안녕하세요')`, `Icon(Icons.star)`, 앱 로고

<!-- end list -->

```dart
// 부모로부터 'name'을 받아 표시하는 정적 위젯
class WelcomeMessage extends StatelessWidget {
  // 1. 데이터는 부모로부터 받으며, final로 선언 (변경 불가)
  final String name;

  // 2. 생성자를 통해 데이터를 받음
  const WelcomeMessage({super.key, required this.name});

  // 3. UI를 그리는 build 메서드
  @override
  Widget build(BuildContext context) {
    return Text(
      '환영합니다, $name님!',
      style: TextStyle(fontSize: 20),
    );
  }
}
```

##### ⏰ StatefulWidget (상태 있는 위젯)

> "사용자 인터랙션이나 데이터 변경으로 인해 스스로 화면을 업데이트하는 동적인 위젯" (예: 시계, 카운터)

  * **특징**: 내부에 변경 가능한 **'상태(State)'를 가집니다.**
  * **구조**: `Widget` 클래스와 `State` 클래스, **두 개가 한 쌍**으로 작동합니다.
  * **핵심 메서드: `setState()`**
      * `State` 클래스 내부의 변수(상태)를 변경할 때 **반드시** 이 메서드로 감싸야 합니다.
      * `setState()`가 호출되면, Flutter는 "상태가 바뀌었으니 `build` 메서드를 다시 실행해서 화면을 새로고침해\!"라고 인식합니다.


```
// 버튼을 누르면 숫자가 올라가는 동적 위젯
class Counter extends StatefulWidget {
  const Counter({super.key});

  @override
  State<Counter> createState() => _CounterState();
}

class _CounterState extends State<Counter> {
  // 1. 위젯이 관리할 내부 상태 변수
  int _count = 0;

  // 2. 상태를 변경하는 로직
  void _increment() {
    // 3. setState()로 상태 변경을 Flutter에 알림
    setState(() {
      _count++; // 이 변수가 변경됨
    });
  }

  // 4. build 메서드 (setState가 호출되면 다시 실행됨)
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Count: $_count'), // 5. 변경된 _count 값이 UI에 반영됨
        ElevatedButton(
          onPressed: _increment, // 6. 버튼 클릭 시 로직 실행
          child: Text('Add'),
        ),
      ],
    );
  }
}
```

-----



#### 3\. API 호출 방법 (http 패키지) (7분)

4차시에서 Node.js로 API를 만들었으니, Flutter에서 `http` 패키지를 사용해 이 API를 호출해 보겠습니다.

**1. `pubspec.yaml`에 패키지 추가**

```yaml
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1 # API 호출을 위한 http 패키지
```

**2. `StatefulWidget`에서 API 호출 (GET 요청)**

API 호출은 시간이 걸리는 비동기 작업이므로, `StatefulWidget`의 `initState` (위젯 생성 시 1회 실행)에서 호출하고, `setState`로 결과를 반영합니다.

```dart
import 'package:flutter/material.dart';
import 'package:http/http.dart' as http; // http로 import
import 'dart:convert'; // json.decode를 위해 import

class TodoWidget extends StatefulWidget {
  const TodoWidget({super.key});

  @override
  State<TodoWidget> createState() => _TodoWidgetState();
}

class _TodoWidgetState extends State<TodoWidget> {
  String _todoTitle = '로딩 중...'; // 1. 상태 변수

  @override
  void initState() {
    super.initState();
    _fetchTodo(); // 2. 위젯 생성 시 API 호출
  }

  // 3. 4차시의 async/await와 동일한 비동기 함수
  Future<void> _fetchTodo() async {
    try {
      // 4차시에서 만든 로컬 서버 주소 (예시)
      final response = await http.get(
        Uri.parse('http://localhost:3000/api/todos/1'),
      );

      if (response.statusCode == 200) {
        // 4. 성공 시, JSON을 파싱하고 setState로 화면 업데이트
        final data = json.decode(response.body);
        setState(() {
          _todoTitle = data['title']; // 'title' 필드 값으로 상태 변경
        });
      } else {
        setState(() { _todoTitle = '데이터 로드 실패'; });
      }
    } catch (e) {
      setState(() { _todoTitle = '오류 발생: $e'; });
    }
  }

  @override
  Widget build(BuildContext context) {
    // 5. _todoTitle의 현재 값(상태)이 화면에 표시됨
    return Text(_todoTitle);
  }
}
```

-----

#### 4\. Supabase 패키지 사용 방법 (6분)

5차시 커리큘럼의 **Supabase**는 'Backend as a Service'입니다. 4차시처럼 직접 백엔드를 구축하는 대신, Supabase가 제공하는 DB와 API를 `supabase_flutter` 패키지로 훨씬 쉽게 사용할 수 있습니다.

**1. `pubspec.yaml`에 패키지 추가**

```yaml
dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0 # Supabase 패키지
```

**2. `main.dart`에서 Supabase 초기화**

앱 실행 전, `main` 함수에서 Supabase URL과 키로 딱 한 번 초기화합니다.

```dart
import 'package:flutter/material.dart';
import 'package:supabase_flutter/supabase_flutter.dart';

Future<void> main() async {
  // 1. Flutter 엔진 초기화 보장
  WidgetsFlutterBinding.ensureInitialized();

  // 2. Supabase 클라이언트 초기화
  await Supabase.initialize(
    url: 'YOUR_SUPABASE_URL', // Supabase 프로젝트 URL
    anonKey: 'YOUR_SUPABASE_ANON_KEY', // Supabase anon 키
  );
  
  runApp(const MyApp());
}
```

**3. 위젯에서 Supabase 클라이언트로 CRUD 작업**

`http` 패키지보다 훨씬 직관적입니다. 4차시의 CRUD 개념과 1:1로 매칭됩니다.

```dart
// 1. Supabase 클라이언트 인스턴스 가져하기
final supabase = Supabase.instance.client;

// 2. CREATE (POST) - 새 북마크 추가
Future<void> createBookmark() async {
  await supabase.from('bookmarks').insert({
    'url': 'https://n8n.io',
    'title': 'n8n - Workflow Automation',
    'category': 'Automation'
  });
}

// 3. READ (GET) - 데이터 조회
Future<void> readBookmarks() async {
  // 'bookmarks' 테이블에서 'category'가 'AI'인 데이터만 조회
  final List<dynamic> data = await supabase
      .from('bookmarks')
      .select('title, url') // 'title'과 'url' 컬럼만
      .eq('category', 'AI'); // 'category' == 'AI'

  // 데이터를 state 변수에 저장 (setState 사용)
  setState(() {
    // _bookmarks = data;
  });
}

// 4. UPDATE (PUT/PATCH) - 1번 ID 북마크 수정
Future<void> updateBookmark(int id) async {
  await supabase
      .from('bookmarks')
      .update({'title': 'New Updated Title'}) // 수정할 내용
      .eq('id', id); // 조건
}

// 5. DELETE (DELETE) - 1번 ID 북마크 삭제
Future<void> deleteBookmark(int id) async {
  await supabase
      .from('bookmarks')
      .delete()
      .eq('id', id); // 조건
}
```

**핵심**: `supabase_flutter` 패키지를 사용하면, `http` 호출, `json` 변환, 에러 처리 등 4차시의 복잡한 백엔드 통신 과정을 추상화된 메서드(`select`, `insert` 등)로 매우 간단하게 처리할 수 있습니다.

### 5. VSCode 플러그인 셋업 (2분)

VSCode 마켓플레이스에서 다음 두 가지를 설치합니다.

1. **`Dart`**
2. **`Flutter`** (Dart를 자동으로 포함)

∗ UI GRID 표시 
-> Preferences: Open User Settings (JSON)
``
`"dart.previewFlutterUiGuides": true`
    

### 6. '오늘의 명언' 앱 실습 (3분)

Cursor Plan Mode에서 하기 내용 입력 진행해주세요 

```

flutter 오늘의 명언앱을 만들고 싶어요.
- display화면과 history 화면으로 구성
- 명언 조회 api 활용과 supabase 통한 history 저장/조회 
- 하단 새로 고침 버튼과 history 버튼  
- 목적은 플러터를 처음배우는 사람들에게 개념을 알려주기 위한 것 
- 화려하지 않아도 되고 widget 개념 ( state, stateless 포함되면 좋음)
- 오늘의 명언을 랜덤으로 가져오는 API 
'''
https://korean-advice-open-api.vercel.app/api/advice
output 
{
  "author": "에이브러햄 링컨",
  "authorProfile": "미국 16대 대통령",
  "message": "자유는 결코 인간이 타고난 것이 아니다. 그것은 인내와 용기와 희생을 통해 얻어지는 것이다."
}
'''

- supabase key 정보 환경 변수 저장할 것 
- supabase key 정보 
  - URL
  https://xxx.supabase.co
  - anon key
    anonkey 
  
```


### 7. Flutter DevTools (2분)

- **Hot Reload (핫 리로드):** (Windows/Linux: `Ctrl+S`, Mac: `Cmd+S`) 코드 저장 즉시 UI가 변경됩니다. 개발 속도를 극적으로 높여줍니다.
    
- **Widget Inspector (위젯 검사기):** VSCode나 Android Studio에서 실행하여, 현재 화면이 어떤 위젯 트리로 구성되어 있는지 시각적으로 분석할 수 있습니다.
    

## 15-2. Upstage OCR API 셋업 (3분)

- **목표:** 명함 이미지에서 텍스트를 추출(OCR)하기 위한 API 키를 발급받습니다.
    
- **과정:**
    
    1. [Upstage AI Console](https://console.upstage.ai/ "null")에 가입합니다.
        
    2. 'Document AI' > 'API Key' 메뉴에서 API 키를 발급받습니다.
        
- **적용:**
    
    - Flutter 프로젝트 루트에 `.env` 파일을 생성합니다. (Node.js의 `.env`와 동일)
        
    - `.env` 파일 안에 발급받은 키를 저장합니다.
        
    
    ```
    UPSTAGE_API_KEY=up_xxxxxxxxxxxxxxxxx
    ```
    
    - (명함 인식 앱 소스 `lib/utils/constants.dart`에서 이 키를 `flutter_dotenv` 패키지를 통해 불러옵니다.)
        

## 15-3. n8n 명함 워크플로우 셋업 (5분)

- **목표:** Flutter 앱에서 전송한 명함 데이터를 받아 DB에 저장하고, 텔레그램으로 알림을 보냅니다.
    
- **필요한 n8n 노드:**
    
    1. **Webhook (Start):**
        
        - Flutter 앱으로부터 `POST` 요청(JSON)을 받습니다.
            
        - `Test URL`을 복사해 둡니다. (이 주소를 `.env` 파일에 `N8N_WEBHOOK_URL`로 저장)
            
    2. **Data Table (or Supabase, Google Sheets):**
        
        - Webhook에서 받은 데이터를 저장합니다. (예: `name`, `email`, `phone` 등)
            
    3. **Telegram:**
        
        - 저장이 완료되면 알림을 보냅니다. (예: `Expression: "명함 등록 완료: {{ $json.body.name }}"`)
            
- **테스트 (Webhook):**
    
    - `curl`이나 Postman을 사용하여 Webhook의 `Test URL`로 아래 샘플 데이터를 `POST`로 전송해 봅니다. 워크플로우가 끝까지 실행되면 성공입니다.
        
    
    ```
    {
      "name": "진주성",
      "email": "joosung@elanvital.ai",
      "phone": "010-9530-0542",
      "company": "ElanvitalAI",
      "position": "CEO",
      "website": "elanvital.ai",
      "address": "경기도 수원 영통구 도청로10"
    }
    ```
    

## 15-4. 모바일 앱 통한 명함 인식 (20분)

### 1. PRD 및 앱 흐름 리뷰

PRD에 정의된 핵심 흐름은 다음과 같습니다.

1. **HomeScreen:** [카메라] 또는 [갤러리] 버튼으로 이미지 선택 (`image_picker` 패키지 사용)
    
2. RecognitionScreen:
    
    a. 선택된 이미지를 OCRService를 통해 Upstage API로 전송.
    
    b. API가 반환한 텍스트를 _parseOCRResponse로 파싱하여 폼(TextField)에 채움.
    
    c. 사용자가 폼을 수정.
    
    d. [Send] 버튼 클릭.
    
3. n8n 연동:
    
    a. N8nService가 폼 데이터를 JSON으로 변환.
    
    b. n8n Webhook URL로 POST 요청 전송.
    

### 2. 소스 구조 분석 (bzcard 앱)

`bzcard` 앱은 '오늘의 명언' 앱보다 체계화된 구조(Service Layer)를 가집니다.

- `pubspec.yaml`:
    
    - `image_picker`: 카메라/갤러리 접근
        
    - `http`: API 통신
        
    - `flutter_dotenv`: `.env` 파일 로드
        
- `lib/utils/constants.dart`:
    
    - `.env` 파일에서 `UPSTAGE_API_KEY`와 `N8N_WEBHOOK_URL`를 로드합니다. (중요!)
        
- `lib/screens/home_screen.dart` (Stateful):
    
    - `_pickImage(ImageSource source)`: `ImagePicker`로 이미지를 선택합니다.
        
    - `Navigator.push(...)`: 이미지 선택 성공 시, 이미지 경로(`image.path`)를 `RecognitionScreen`으로 넘겨주며 화면을 이동시킵니다.
        
- `lib/screens/recognition_screen.dart` (Stateful):
    
    - `initState()`: `HomeScreen`에서 넘겨받은 이미지로 `_recognizeCard()`를 즉시 호출.
        
    - `_recognizeCard()`:
        
        - `setState(() { _isLoading = true; });`
            
        - `OCRService.recognizeBusinessCard(...)` 호출.
            
        - 반환된 `BusinessCard` 객체로 `TextEditingController`들 (폼)을 채움.
            
    - `_sendToN8n()`:
        
        - `setState(() { _isSending = true; });`
            
        - `N8nService.sendBusinessCard(...)` 호출.
            
        - 성공 시 `Navigator.pop(context)`로 홈 화면 복귀.
            
- **`lib/services/ocr_service.dart` (★핵심 1★):**
    
    - `recognizeBusinessCard(...)`:
        
        - `http.MultipartRequest`를 생성합니다. (파일을 업로드해야 하므로 `http.post`가 아님)
            
        - 헤더에 `Authorization: Bearer ${Constants.upstageApiKey}`를 추가합니다.
            
        - `http.MultipartFile.fromPath('document', file.path)`로 이미지 파일을 요청에 첨부합니다.
            
        - API 응답(JSON)을 `_parseOCRResponse`로 넘깁니다.
            
    - `_parseOCRResponse(...)`:
        
        - **(중요)** Upstage API는 명함의 필드를 구분하지 않고, 인식된 **전체 텍스트**를 반환합니다.
            
        - 이 함수는 간단한 정규식(RegExp)과 키워드(예: 'Email', '@', 'M:', 'T:')를 사용해 전체 텍스트에서 이메일, 전화번호 등을 "추측"하여 `BusinessCard` 객체로 만듭니다. (AI 시대에 이런 파싱 작업이 중요합니다)
            
- **`lib/services/n8n_service.dart` (★핵심 2★):**
    
    - `sendBusinessCard(BusinessCard card)`:
        
        - `RecognitionScreen`에서 정리된 `BusinessCard` 객체를 받습니다.
            
        - `card.toJson()`으로 JSON 문자열로 변환합니다.
            
        - `http.post(Uri.parse(Constants.n8nWebhookUrl), ...)`를 통해 n8n Webhook으로 JSON 데이터를 전송합니다.
            

### 3. 시뮬레이터 실행 및 배포 안내

- **실행 방법:**
    
    1. 프로젝트 루트에 `.env` 파일을 생성하고 `UPSTAGE_API_KEY`와 `N8N_WEBHOOK_URL`을 입력합니다.
        
    2. 터미널에서 `flutter pub get`을 실행하여 패키지를 설치합니다.
        
    3. Android Studio 또는 Xcode에서 시뮬레이터를 실행합니다.
        
    4. `flutter run` 또는 VSCode의 [Run > Start Debugging]으로 앱을 실행합니다.
        
- **실제 폰에 설치하기:**
    
    - **Android:** `flutter build apk` 명령어로 `.apk` 설치 파일을 생성하여 폰에 직접 설치할 수 있습니다.
        
    - **iOS:** Apple Developer 계정(유/무료)이 필요하며, Xcode를 통해 실제 기기를 연결하고 빌드해야 합니다. (과정 GKA)